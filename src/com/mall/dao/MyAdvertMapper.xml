<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.dao.MyAdvertMapper">
	<!-- 查询首页主广告, 主广告下小广告, 热卖右侧广告查询-->
	<select id="queryHomeAdverts" resultType="Advert">
		(select name, img, pos from advert where pos = 'HomeMain'
		ORDER BY sort ASC
		LIMIT 4)
		UNION ALL
		(select name, img, pos from advert where pos = 'HomeMainDown'
		ORDER BY sort ASC
		LIMIT 3)
		UNION ALL
		(select name, img, pos from advert where pos = 'HotSellRight'
		ORDER BY sort ASC
		LIMIT 1)
	</select>
</mapper>